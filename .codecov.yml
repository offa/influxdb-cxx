codecov:
  notify:
    after_n_builds: 1

coverage:
  precision: 2
  round: nearest
  range: 60...90
  status:
    project: true
    patch: false
    changes: false

ignore:
  - cmake/*
  - test/*
  - src/HTTP.*
  - src/UDP.*
  - src/UnixSocket.*

comment: false
